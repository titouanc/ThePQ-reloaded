#ifndef ServerMatchManager_hpp
#define ServerMatchManager_hpp

#include "MatchManager.hpp"

class ServerMatchManager : MatchManager {
public:
	void addStroke(Team sender, Stroke moves;
	void sendTurn(Stroke turn[]);
};

#endif