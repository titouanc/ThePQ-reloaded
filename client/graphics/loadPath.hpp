#ifndef DEFINE_LOADPATH_HEADER
#define DEFINE_LOADPATH_HEADER

#include <string>

std::string fontPath(std::string const & name);

std::string texturePath(std::string const & name);

std::string soundPath(std::string const & name);

#endif